#ifndef MAIN_H
#define MAIN_H
/* C Libraries used */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/wait.h>

/* Prototypes used */
char *strtok(char *str, const char *delim);
char *_strtok(char *str, char *delim);
#endif
